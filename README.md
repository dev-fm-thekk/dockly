# dockly

Lightweight visual Docker TUI.

# operations

A dashboard that shows the following details:
docker engine status: on or off
auth status: logged in user
running containers:
running images:

Detailed view of images disk-space, process-id, actions: kill, start, stop
"""" same for containers and volumes

disk usage of docker, cache
